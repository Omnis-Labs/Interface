import { PortfolioSummary } from "@/types/strategy"

interface DeploymentSummaryProps {
    portfolioSummary: PortfolioSummary
}

export const DeploymentSummary = ({ portfolioSummary }: DeploymentSummaryProps) => {
    return (
        <div>
            hello
        </div>
    )
}